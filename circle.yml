---
machine:
  services:
    - docker

dependencies:
  override:
    - make deps-circle

test:
  override:
    - make build-docker

deployment:
  production:
    branch:
      - master
      - release-test
    commands:
      - make push
      - make push-master-tag
